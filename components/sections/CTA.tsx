import { Mail, MessageCircle, Send, Phone } from "lucide-react";
import { Container } from "@/components/ui/Container";
import { siteConfig } from "@/lib/site";
const contacts = [{ label: "Email", value: siteConfig.contact.email, href: `mailto:${siteConfig.contact.email}`, icon: Mail }, { label: "Telegram", value: siteConfig.contact.telegram, href: `https://t.me/${siteConfig.contact.telegram}`, icon: Send }, { label: "Line", value: siteConfig.contact.line, href: "#", icon: MessageCircle }, { label: "WhatsApp", value: siteConfig.contact.whatsapp, href: `https://wa.me/${siteConfig.contact.whatsapp.replace(/\s/g,"")}`, icon: Phone }];
export function CTA() {
  return <section id="contact" className="bg-primary py-24 text-white dark:bg-slate-950"><Container><div className="mx-auto max-w-4xl text-center"><Send className="mx-auto mb-6 text-brandCyan" size={36} /><h2 className="text-3xl font-bold md:text-4xl">Build Real Products With AI.</h2><p className="mt-4 text-lg leading-8 text-slate-300">Request a demo, join the early access list, or ask about implementation support.</p><div className="mt-10 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{contacts.map((item) => { const Icon = item.icon; return <a key={item.label} href={item.href} target={item.href.startsWith("http") ? "_blank" : undefined} rel={item.href.startsWith("http") ? "noopener noreferrer" : undefined} className="rounded-2xl border border-white/10 bg-white/5 p-5 text-left transition hover:bg-white/10"><Icon className="mb-4 text-brandCyan" size={24} /><p className="font-semibold text-white">{item.label}</p><p className="mt-1 break-all text-sm text-slate-300">{item.value}</p></a>; })}</div></div></Container></section>;
}
