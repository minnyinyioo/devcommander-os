import { Container } from "@/components/ui/Container";
import { SectionTitle } from "@/components/ui/SectionTitle";
export function Founder() {
  return <section className="bg-softBg py-24 dark:bg-slate-900"><Container><SectionTitle eyebrow="Why It Exists" title="Built for the Reality of AI Product Development" description="AI can write code, but real products fail because context is lost, scope expands, verification is weak and handoff is messy. DevCommander OS is designed to protect the entire product journey." /></Container></section>;
}
